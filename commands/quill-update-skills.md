---
description: Update Quill skill bundle from remote source, preserving user-modified files
allowed-tools: Bash
---

# /quill-update-skills

执行：

```bash
bash ${CLAUDE_PLUGIN_ROOT}/lib/skill-update.sh
```

展示输出（包含跳过用户改过的文件清单）。
