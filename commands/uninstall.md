---
description: Quill · 卸载（清当前项目产物 + 可选清全局缓存 + 调 claude CLI 卸 plugin）
argument-hint: "[--global] [--yes] [--dry-run]"
allowed-tools: Bash
---

# /quill:uninstall · 卸载

执行：

```bash
bash ${CLAUDE_PLUGIN_ROOT}/lib/uninstall.sh $ARGUMENTS
```

完整说明见 [README 的 Uninstall 段](../README.md#uninstall)。
